# Public desktop builds read this file (bundled as gd.env). No Riot key here.
# After you deploy the proxy, paste its HTTPS URL:
GD_API_URL=
# Optional shared token — must match GD_APP_TOKEN on the server.
GD_APP_TOKEN=
