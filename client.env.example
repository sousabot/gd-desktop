# Public desktop builds read this file (bundled as gd.env). No Riot key here.
# After you deploy the proxy, paste its HTTPS URL:
GD_API_URL=https://gd-desktop.onrender.com
# Must match GD_APP_TOKEN on the server. `npm run dist` fills this if empty.
GD_APP_TOKEN=
