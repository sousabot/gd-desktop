# Public desktop builds read this file (bundled as rift.env). No Riot key here.
# After you deploy the proxy, paste its HTTPS URL:
RIFT_API_URL=https://gd-desktop.onrender.com
# Must match RIFT_APP_TOKEN on the server. `npm run dist` fills this if empty.
RIFT_APP_TOKEN=
